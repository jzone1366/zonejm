<!--
<template>
	<div class="flex flex-col items-center p-8 rounded-md w-60 sm:px-12 dark:bg-gray-50 dark:text-gray-800">
		<div class="text-center">
            <h2 class="text-xl font-semibold">{{ data.location.name}} </h2>
            <p class="text-sm dark:text-gray-600">{{ formattedDate }}</p>
		</div>
        <NuxtImg :src="`https:${data.current.condition.icon}`" />
        <div class="mb-2 text-3xl font-semibold">{{ Math.round(data.current.temp_f) }}°
            <span class="mx-1 font-normal">/</span> {{ Math.round(data.current.temp_c) }}°
		</div>
        <p class="dark:text-gray-600">{{data.current.condition.text}}</p>
	</div>
</template>

<script setup>
//const config = useRuntimeConfig()
//console.log(config.public.functionBaseUrl)
//
//const res = await fetch(`${config.public.functionBaseUrl}/.netlify/functions/weather`)
//const data = await res.json()
//const thisDate = new Date(data.location.localtime)
//const formattedDate = thisDate.toLocaleString('default', { month: 'short', day: '2-digit'})
//
</script>
-->

<!-- Placeholder empty template to prevent Vue errors -->
<template>
  <div></div>
</template>
